for (i in 1:length(cl)){
    
    system(paste('scp expo_dep_inst.sh philipp@',cl[i],':/home/philipp/',sep=''))
    
    system(paste('ssh',cl[i],'chmod 700 fastin_dep_install.sh'))

    system(paste('ssh',cl[i],'./fastin_dep_install.sh'))

}
